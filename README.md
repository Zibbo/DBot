# DBot

## Setup

1. Copy `config.example.py` to `config.py` and add your real `DISCORD_TOKEN`, `OPENAI_API_KEY`, and any trigger words/phrases you want the bot to respond to.
2. Install dependencies and run `python bot.py`.
